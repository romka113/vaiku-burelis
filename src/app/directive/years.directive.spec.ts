import { YearsDirective } from './years.directive';

describe('YearsDirective', () => {
  it('should create an instance', () => {
    const directive = new YearsDirective();
    expect(directive).toBeTruthy();
  });
});
